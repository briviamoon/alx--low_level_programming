#include "main.h"
/**
 *print_line - prints a line.
 *@n: argument stating number of lines.
 */

void print_line(int n)
{
	int lineNumb = 0;
	char line = '_';

	if (n > lineNumb)
	{
		for (lineNumb = 0; lineNum < n; lineNum++)
		{
			_putchar(line);
		}
		_putchar('\n');
	}
	else
	{
		_putchar('\n');
	}
}
