#include "main.h"
/**
 *_abs - prints the absolute value of a number.
 *Return: val when successful.
 */

int _abs(int)
{
	if (int < 0 )
	{
		int val = int * -1;
		return (val);
	}
	else if (int > 0)
	{
		int val = int * 1;
		return (val);
	}
	else
	{
		return (val);
	}
}
